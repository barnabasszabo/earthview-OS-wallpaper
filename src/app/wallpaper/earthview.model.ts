
export interface EarthviewOptions {

  screen: string | number;

  scale: string // 'auto' | 'fill' | 'fit' | 'stretch' | 'center'

}

export interface SlugChainData {
  slug: string;
  photoUrl: string;
  name: string;
}
